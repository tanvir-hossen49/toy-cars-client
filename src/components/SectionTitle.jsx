const SectionTitle = ({ title }) => {
  return (
    <h2 className="md:w-1/2 w-full mx-auto capitalize text-center font-bold md:text-4xl text-3xl mb-5 section-header">
      {title}
    </h2>
  );
};

export default SectionTitle;
